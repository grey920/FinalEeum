<?xml version="1.0" encoding="UTF-8" ?>
<!-- SQL 맵퍼 파일은 xml이기 때문에 제일 먼저 xml 선언이 옵니다. -->
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="eeum">

	<select id="allUsers" resultType="int">
		WITH USERS AS
		(
		SELECT USER_ID FROM USER_TABLE
		UNION ALL
		SELECT EXPERT_ID FROM EXPERT_TABLE
		)
		SELECT COUNT(NVL(USER_ID,0)) FROM USERS
	</select>

	<select id="deExperts" resultType="Portfolio" parameterType="String">
		SELECT
		PF_EXID, PF_LOC, PF_GRADE, PF_CATE, PF_TIME,
		PF_DESC,
		PF_SV_LI, PF_SV_OP, PF_INTRO, PF_One
		FROM PORTFOLIO
		WHERE
		PF_EXID=#{PF_EXID}
	</select>

	<select id="allReviews" resultType="int">
		SELECT COUNT(NVL(RV_INDEX,0))
		FROM
		REVIEW_BOARD
	</select>

	<select id="cPosts" resultType="int">
		SELECT COUNT(NVL(RV_INDEX, 0))
		FROM REVIEW_BOARD
		WHERE PODATE BETWEEN SYSDATE-1 AND SYSDATE
	</select>

	<select id="newUsers" resultType="int">
		WITH USERS AS
		(
		SELECT USER_ID,
		JOINDATE FROM USER_TABLE WHERE JOINDATE BETWEEN SYSDATE-1
		AND SYSDATE
		UNION ALL
		SELECT EXPERT_ID, JOINDATE FROM EXPERT_TABLE WHERE JOINDATE
		BETWEEN
		SYSDATE-1 AND SYSDATE
		)
		SELECT COUNT(NVL(USER_ID, 0)) FROM USERS
	</select>
	
	<select id="reportex" resultType="Reportex">
		SELECT REPEX_EXID, REPEX_CATE, REPEX_STATE
		FROM REPORTEX 
	</select>
	
	<select id="repu" resultType="Repu">
		SELECT REPU_UID, REPU_CATE, REPU_STATE
		FROM REPU
	</select>
	
	<!-- <select id="crepex" resultType="int">
		SELECT REPEX_EXID, COUNT(REPEX_EXID) 
		FROM REPORTEX 
		GROUP BY REPEX_EXID
	</select> 
	
	
	<select id="crepu" resultType="int">
		SELECT REPU_UID, COUNT(REPU_UID) 
		FROM REPU
		GROUP BY REPU_UID
	</select>-->
	
</mapper>