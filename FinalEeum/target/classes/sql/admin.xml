<?xml version="1.0" encoding="UTF-8" ?>
<!-- SQL 맵퍼 파일은 xml이기 때문에 제일 먼저 xml 선언이 옵니다. -->
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="eeum">

	<select id="allUsers" resultType="int">
		WITH USERS AS
		(
		SELECT USER_ID FROM USER_TABLE
		UNION ALL
		SELECT EXPERT_ID FROM EXPERT_TABLE
		)
		SELECT COUNT(NVL(USER_ID,0)) FROM USERS
	</select>

	<select id="deExperts" resultType="Portfolio" parameterType="String">
		SELECT
		PF_EXID, PF_LOC, PF_GRADE, PF_CATE, PF_TIME,
		PF_DESC,
		PF_SV_LI, PF_SV_OP, PF_INTRO, PF_One
		FROM PORTFOLIO
		WHERE
		PF_EXID=#{PF_EXID}
	</select>

	<select id="allReviews" resultType="int">
		SELECT COUNT(NVL(RV_INDEX,0))
		FROM
		REVIEW_BOARD
	</select>

	<select id="cPosts" resultType="int">
		SELECT COUNT(NVL(RV_INDEX, 0))
		FROM REVIEW_BOARD
		WHERE PODATE BETWEEN SYSDATE-1 AND SYSDATE
	</select>

	<select id="newUsers" resultType="int">
		WITH USERS AS
		(
		SELECT USER_ID,
		JOINDATE FROM USER_TABLE WHERE JOINDATE BETWEEN SYSDATE-1
		AND SYSDATE
		UNION ALL
		SELECT EXPERT_ID, JOINDATE FROM EXPERT_TABLE WHERE JOINDATE
		BETWEEN
		SYSDATE-1 AND SYSDATE
		)
		SELECT COUNT(NVL(USER_ID, 0)) FROM USERS
	</select>
	
	<!-- 관리자 사이트에서 신고 회원 -->
	<select id="qna" resultMap="qnaMap">
		SELECT QNA_INDEX, QNA_WRITER, QNA_REPORT, QNA_TYPE, QNA_TIMES
		FROM QNABOARD
	</select>
	<resultMap id="qnaMap" type="com.kh.eeum.domain.Repu">
		<result property="QNA_REPORT" column="QNA_REPORT" />
		<collection property="qnalist" ofType="com.kh.eeum.domain.QnA">
			<result property="QNA_INDEX" column="QNA_INDEX" />
			<result property="QNA_WRITER" column="QNA_WRITER" />
			<result property="QNA_REPORT" column="QNA_REPORT" />	
			<result property="QNA_TYPE" column="QNA_TYPE"/>
			<result property="QNA_TIMES" column="QNA_TIMES"/>
		</collection>
	</resultMap>
	
</mapper>